# PENDING — بنود معلّقة في القرآن الكريم

> الذاكرة الخارجية للبنود التي لم تُنفَّذ بعد. اقرأه قبل أي تعديل.

## ⏳ ترحيل القرآن الكريم إلى المعيار المتجاوب الموحّد

> كُتب في 2026-09-17 من محادثة مَوْلِدي. **لا يُنفَّذ إلا في محادثة القرآن الكريم.**
> المعيار: `~/Claude Workspace/RESPONSIVE_STANDARD.md` — الأدوات في
> `~/Claude Workspace/shared/` (`responsive.css`, `lint-responsive.py`,
> `overflow-harness.html`). أُنجز على 007.gallery ومَوْلِدي.

**الحالة المقيسة (شُغِّل المدقّق فعلاً بلا تعديل أي ملف):**

    python3 ../shared/lint-responsive.py .
    ✗ 25 خرقاً في ملف واحد — index.html

الموقع صفحة واحدة بأنماط مضمَّنة، فالخروق كلها في `index.html`.

**تركيبة الخروق — النصف الأخطر ليس نقاط التوقف:**

- **10 خروق `left`/`right` مطلقة** تكسر RTL. هذه في **مصحف عربي**، أي أن
  الاتجاه ليس تفصيلاً تجميلياً بل صلب التجربة. عالجها أولاً بـ`inset-inline-*`.
- **15 نقطة `max-width`**: 360, 430, 520×5, 560, 600, 640×2, 820, 860, 900.
  الموقع مكتوب تنازلياً (desktop-first) مثل أوراقنا، فالعمل قلب منهج لا نقل
  نقاط. تُطوى على السلّم 600 / 1024 / 1440.

**⚠️ الخطر الأكبر — service worker يحجب أي إصلاح:**

`sw.js` يخزّن بمفتاح `CACHE = 'qurankarem-v71'`. ما دام الرقم ثابتاً فإن
الزائر الذي فتح الموقع مرة **لن يرى أي تعديل CSS أو HTML إطلاقاً** — لا بعد
ساعة ولا بعد يوم. هذا أشدّ من مشكلة التخزين العادية التي واجهناها في مَوْلِدي
(`max-age=86400`)، لأن الـservice worker يقدّم من الكاش قبل أن يسأل الشبكة.

**لذلك: كل دفعة تعديل في هذا المشروع تُرفَق برفع رقم `CACHE`.** ولا يُعتدّ بأي
تحقّق بصري قبل رفعه — ستقيس نسخة قديمة وتظنّها الجديدة.

**درسان مكلّفان من مَوْلِدي:**

1. **ممنوع `@import`** داخل ملفات CSS. الملف المستورد يبقى رهينة نسخة
   المستورِد المخزَّنة. اربط بـ`<link>` مع بصمة `?v=` من محتوى الملفات.
2. **مصدر واحد للكتلة ولأنماطها معاً.** توحيد الـHTML وحده لا يكفي: في مَوْلِدي
   وُحِّد الـHTML وبقيت الأنماط مكرّرة، فظهرت كتلة بلا تنسيق في صفحتين.

**معيار النجاح (الثلاثة معاً، قابلة للتشغيل):**

    python3 ../shared/lint-responsive.py .     → صفر خرق
    مِقياس الفيضان على ٦ عروض                   → صفر فيضان
    هدف اللمس في كل عنصر تفاعلي                 → ≥ 44px

**تحذير من أداة القياس:** `overflow-harness.html` القديم كان يفحص
`documentElement` وحده فأعطى «صفر فيضان» لصفحة تفيض 105px، لأن
`overflow-x:hidden` يُخفي القياس. النسخة الحالية في `shared/` تأخذ الأكبر بين
`documentElement` و`body`. استعمل المحدَّثة.

**بند قديم لا يزال قائماً:** تحويل الطبقات المنبثقة إلى صفحات مستقلة
(ذاكرة `goal-popups-to-pages`) — لم يبدأ في هذا المشروع. يُنسَّق مع الترحيل
المتجاوب لأن كليهما يمسّ البنية نفسها.
